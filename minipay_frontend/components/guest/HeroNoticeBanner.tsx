import NoticeBanner from "@/components/ui/NoticeBanner";

/** Dismissible notice strip above the mobile nav on the hero — fed by heroPageErrors helpers. */
export default function HeroNoticeBanner() {
  return <NoticeBanner placement="hero" />;
}
