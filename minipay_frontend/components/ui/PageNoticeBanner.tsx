import NoticeBanner from "@/components/ui/NoticeBanner";

/** Dismissible notice strip for settings / setup pages (e.g. play-ai-3d). */
export default function PageNoticeBanner() {
  return <NoticeBanner placement="page" />;
}
